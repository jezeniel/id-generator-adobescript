ID generator AdobeScript v0.3 (JavaScript)

1. Create a PSD File with layer names like this:

	PhotoFormat -> Art Layer this will be a reference 
		       for your ID photo's style and position
	IDName  -> Text Layer
	IDNum   -> Text Layer
	Email   -> Text Layer
	Contact -> Text Layer
	MemberType -> Text Layer 

    note: 
	    -Name your psd as ID.psd.
	 
	    -your photos must be all resized because the photo auto resize
	     feature is not yet available
	
	    -your Photoshop's preferences units must be set to PIXELS
	
	    -id photos must be jpg files
	
2. Create an excel file and input data in this order.
   | NAME  | MEMBERTYPE | IDNUM | CONTACT | EMAIL |

3. Save your excel file as *.txt (TAB DELIMITED)

4. Put all your ID photos, textfile and PSD in the a single folder.
	ex. 
		My Folder -
			|- pic1.jpg
			|- pic2.jpg
			|- memberlist.txt
			|- ID.psd
						  
5. run the script and select the source folder and save location

	
